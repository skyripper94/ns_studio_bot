[phases.setup]
# системные шрифты для рендеринга; локальные TTF/OTF из `fonts/` будут включены в сборку из репозитория
aptPkgs = ["fonts-dejavu-core", "fonts-liberation", "fontconfig", "fonts-noto"]

